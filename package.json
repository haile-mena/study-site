{
  "name": "study-room-app",
  "version": "1.0.0",
  "description": "Real-time collaborative study room app",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
